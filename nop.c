#include "monty.h"
/**
 * fnop - is a function that implements nothing
 * @head: is a pointer to the first node
 * @line_number: is the line being read from
 * Return: void
 */
void fnop(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}
